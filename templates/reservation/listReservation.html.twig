{% extends 'base.html.twig' %}


{% block javascripts %}

            <!-- Template Main JS File -->
<script src="{{ asset('assets/js/jquery.min.js') }}"></script>
{# <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script> #}
{# <script src="{{ asset('assets/js/jquery.js') }}"></script> #}
{# <link href="../../web/css/bootstrap.min.css" rel="stylesheet"> #}

<script src="{{ asset('assets/js/reservation.js') }}"></script>
        
{% endblock %}
{% block sidebar %}
{% endblock %}
{% block body %}



<div class="row" style=" width:-100%;margin-left:50px;margin-top: -100px;">

    <div class="col-lg-6">

        <div class="card"  style=" width:1600px;align-items: center;justify-content: center; top: 50%;transform: perspective(1px) translateY(-17%);">
            <div class="card-body"> 
                <a href="{{ path('index') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
                <h5 class="card-title">Liste des Salles de cours</h5>

                <!-- Table with stripped rows -->
                <div style="width:1600px;display: flex; justify-content:left;">
		<table class="table table-bordered"  border="1" width: "10%"  >
                    <thead>
                    <tr align="center"> 
                        <th>N° Salle</th>
                        <th>Batiment</th>
                        <th>Capacité</th>
                        <th>Période</th>
                         <th>Etat</th>
                        <th>Action</th>
                        <th>Localiser</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for reservation in b %}
                    {% for reservation2 in b1 %}
                        <tr class="old gradex">
                            <td align="center">{{reservation.numeroSalle}} </td>
                            <td align="center">{{reservation.nomSalle }}</td>
                            <td align="center">{{ reservation.capacite}}</td>
                            
                            <td>
                                <input type="checkbox" name="m" style="margin-right:15px"/>{{ reservation2.periodeMatin}}
                                </br><input type="checkbox" name="m" style="margin-right:15px"/>{{ reservation2.periodeSoir}}
                                </br><input type="checkbox" name="m" style="margin-right:15px"/>{{ reservation2.periodeTPE}}
                            </td>
                            <td align="center" class="test">{{reservation2.etat}}</td>
                            <td align="center" >
                            {# <p id="test">This is another paragraph.</p> #}
                                <a class="btn btn-primary reserver" href="#" >Reserver</a>
                            </td>
                             <td align="center">
                                <a class="btn btn-secondary" href="#">Géolocoliser</a>
                            </td


                            
                                
                            {% endfor %}
                            
                            {#<td>{{user.accountType}}</td> #}
                            {# <td>
                                <a class="btn btn-danger" href="{{ path('modif_user',{'id':user.id}) }}" style="background-color:blue;">Modifier</a>
                            </td>
                            <td>
                                <a class="btn btn-danger" href="{{ path('supp_user',{'id':user.id}) }}">Supprimer</a>
                            </td> #}
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
		
	</div>
                <!-- End small tables -->

            </div>
        </div>

    </div>



</div>
{% block footer %}
            {% endblock %}
{% endblock %}
